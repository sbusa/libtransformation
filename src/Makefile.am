# BUILD THE LIBRARY
AUTOMAKE_OPTIONS = gnu

if BUILD_CONTRIB
SUBDIRS = contrib include library contrib/ActiveMQ modules programs
else
SUBDIRS = include library contrib/ActiveMQ modules programs
endif
